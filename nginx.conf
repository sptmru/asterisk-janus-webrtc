server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index softphone.html;

  location = / {
    try_files /softphone.html =404;
  }

  location / {
    try_files $uri $uri/ =404;
  }
}
